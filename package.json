{
	"license": "MIT",
	"private": true,
	"scripts": {
		"dev": "FORCE_COLOR=true turbo run dev --parallel",
		"dev:frontend": "FORCE_COLOR=true turbo run dev --filter=@tinseau/frontend --parallel",
		"build": "FORCE_COLOR=true turbo run build",
		"start": "FORCE_COLOR=true turbo run start"
	}
}